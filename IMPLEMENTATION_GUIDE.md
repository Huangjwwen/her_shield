# 她护（Her Shield）新功能实现使用指南

## 📋 目录
1. [3.1 SHA-256 证据指纹](#31-sha-256-证据指纹)
2. [3.2 结构化取证清单](#32-结构化取证清单)
3. [3.3 危机干预弹窗](#33-危机干预弹窗)
4. [3.4 CBT升级+语音引导](#34-cbt升级语音引导)
5. [技术架构](#技术架构)
6. [测试清单](#测试清单)

---

## 3.1 SHA-256 证据指纹

### 功能描述
用户可以上传任意文件（文档、图片、录音等），系统会自动计算该文件的 SHA-256 哈希值，生成带有时间戳的存证凭证，可供下载或截图留存。这个功能用于证明文件在特定时间的原始状态，任何篡改都会导致哈希值变化。

### 使用流程
```
1. 进入"她证·证据保全"页面
   ↓
2. 在上方看到"⭐ SHA-256 证据指纪（防篡改）"拖拽区域
   ↓
3. 拖拽文件或点击选择文件
   ↓
4. 系统即时计算 SHA-256 哈希
   ↓
5. 显示存证凭证卡片：
   ├─ 文件名：[文件名]
   ├─ 文件大小：[大小]
   ├─ 修改时间：[时间]
   ├─ SHA-256 指纹：[64位16进制字符串]
   └─ 证书ID：CERT_[时间戳]
   ↓
6. 三个操作按钮：
   ├─ 📥 下载凭证（JSON格式）
   ├─ 📸 截图凭证
   └─ 🗑️ 删除
```

### 关键函数
- `calculateFileSHA256(file)` - 计算文件SHA-256
- `generateCertificateCard(file, hash)` - 生成凭证卡片HTML
- `downloadCertificate(certId)` - 下载JSON凭证
- `screenshotCertificate(certId)` - 截图凭证
- `initEvidenceFileUpload()` - 初始化文件上传

### 防篡改演示方法
1. 上传原始文件，记下 SHA-256 值
2. 修改文件内容
3. 再次上传修改后的文件
4. 对比两个SHA-256值，确认已完全不同
5. 这证明了文件内容的任何改变都会导致哈希值变化

### 浏览器兼容性
- ✅ Chrome/Edge 63+
- ✅ Firefox 57+
- ✅ Safari 11+
- ❌ IE（不支持）

---

## 3.2 结构化取证清单

### 功能描述
当用户查询证据取证指南时，系统不仅返回文字指南，还会自动将其转换为可勾选的 ToDoList。用户可以逐项勾选，系统实时更新完成进度条。

### 使用流程
```
1. 在"她证·证据保全"输入框中描述维权场景
   例如：招聘时遇到性别歧视，如何收集证据？
   ↓
2. 点击"查询取证指南"
   ↓
3. 获得响应后，历史记录中显示"取证清单"卡片
   ↓
4. 清单包含：
   ├─ 取证方法（可勾选列表）
   ├─ 进度条和完成计数
   ├─ 核心证据类型
   ├─ 取证注意事项
   └─ 证据保存方式
   ↓
5. 用户逐项勾选完成的任务
   ↓
6. 进度条实时更新，显示 X/Y
   ↓
7. 完成100%时，系统显示庆祝提示：🎉 恭喜，取证清单已完成！
```

### 完整示例
```
场景：职场言语性骚扰
取证方法清单：
☐ 使用手机录音功能录制对话
☐ 截图保存暧昧/骚扰性聊天记录
☐ 保存邮件往来中的不当言论
☐ 录屏保存社交媒体上的骚扰内容

进度条：0/4 → 点击勾选 → 1/4 → 2/4 → ... → 4/4 → 🎉
```

### 关键函数
- `renderEvidenceChecklist(record)` - 生成可勾选清单HTML
- `updateEvidenceProgress(checklistId, totalItems)` - 更新进度条

### 样式类名
- `.todolist-container` - 容器
- `.todolist-header` - 标题栏
- `.progress-bar` - 进度条轨道
- `.progress-fill` - 进度条填充
- `.todo-item` - 单个项目
- `.todo-checkbox` - 复选框
- `.todo-text` - 项目文本

---

## 3.3 危机干预弹窗

### 功能描述
系统在后台监听用户在"她心·情绪树洞"的输入，当检测到自杀、自伤等极端关键词时，立即强制显示一个无法忽视的危机干预弹窗，包含24小时心理援助热线和求助资源。

### 关键词表
系统监听四类关键词：

#### 1. 自杀相关
自杀、自杀念头、想死、活不下去、不想活、去死、寻死、投河、跳楼、割腕、喝药、死一死算了、一死了之、了此残生、结束生命

#### 2. 绝望相关
绝望、完全绝望、没有希望、活着没意思、人生无望、无路可走、山穷水尽、彻底失望、这辈子完了

#### 3. 极端痛苦
太痛苦了、活着太难了、受不了了、快疯了、精神崩溃、心如刀割、生不如死、极度痛苦、承受不起

#### 4. 自伤行为
自伤、伤害自己、自残、割自己、打自己、撞墙、惩罚自己、虐待自己

### 使用流程
```
1. 用户在"她心·情绪树洞"输入框中输入文字
   ↓
2. 系统实时监听输入事件
   ↓
3. 检测到危机关键词（任意一个）
   ↓
4. 强制显示危机干预模态框：
   ├─ 标题：🆘 您可能需要帮助
   ├─ 正文：识别和同情
   ├─ 三条24小时热线
   │  ├─ 400-161-9995（北京）
   │  ├─ 010-8295-1332（全国）
   │  └─ 020-8393-0332（广州）
   ├─ 其他求助资源
   │  ├─ 120 急救、110 报警
   │  ├─ 医院精神心理科
   │  ├─ 信任的人倾诉
   │  └─ 单位/学校心理援助
   └─ 温暖的话：💚 您的生命很重要，这个世界因为您而更好
   ↓
5. 关闭按钮：只能点"✕"关闭，点背景无法关闭
   ↓
6. 用户再次输入关键词会重新弹窗
```

### 特殊设计
- **强制性**：使用 `z-index: 3000` 覆盖所有内容
- **无法忽视**：点击背景会提示，只能点关闭按钮
- **实时检测**：在每个字符输入时检测
- **重复触发**：关闭后再输入关键词会再次弹出
- **可配置**：热线号码可在 `CRISIS_KEYWORDS` 之后的模板中修改

### 关键函数
- `hasCrisisKeywords(text)` - 检测关键词
- `showCrisisModal()` - 显示弹窗
- `closeCrisisModal()` - 关闭弹窗

### 样式类名
- `.crisis-modal` - 模态框容器
- `.crisis-content` - 内容区域
- `.crisis-hotline` - 热线卡片
- `.crisis-resources` - 资源列表

### 注意事项
⚠️ **重要**：上线前请逐一核实热线号码的准确性和可用性，确保用户能够真正获得帮助。

---

## 3.4 CBT升级+语音引导

### 功能描述
系统在"她心·情绪树洞"提供基于认知行为疗法（CBT）原理的结构化心理支持，帮助用户识别和重构非理性思维。每条回复旁边都有一个"🎧 语音引导"按钮，用户可以点击让系统用中文语音朗读回复内容。

### CBT 结构（六步）
```
【🧠 情绪识别】
  └─ 帮助用户识别自己的情绪（悲伤、愤怒、焦虑等）

【⚡ 认知模式识别】
  ├─ 绝对化思维：用过于绝对的语言（应该、必须、永远）
  ├─ 灾难化思维：预期最糟的结果（完了、毁了）
  ├─ 标签化：用不变的标签定义自己（我是个失败者）
  └─ 过度自责：承担过多责任

【💡 重新审视】
  ├─ 这个想法是100%准确吗？
  ├─ 其他可能性有哪些？
  ├─ 朋友遇到会怎么看？
  └─ 我有哪些优点和资源？

【🌈 新的视角】
  └─ 构建更平衡、更理性的信念

【🎯 今天可以尝试】
  ├─ 1个小行动：做让自己舒服的事
  ├─ 2个观察：注意两件好事
  └─ 3次呼吸：当感受强烈时深呼吸

【💚 温暖的话】
  └─ 自我同情和鼓励
```

### 语音引导使用流程
```
1. 进入"她心·情绪树洞"，输入倾诉内容
   ↓
2. 系统生成CBT结构的回复并保存到历史记录
   ↓
3. 历史记录中每条回复旁显示"🎧 语音引导"按钮
   ↓
4. 点击按钮
   ↓
5. 系统自动朗读回复内容：
   ├─ 提取有意义的文本（去除markdown）
   ├─ 限制在1000字符以内（避免过长）
   ├─ 使用中文语音合成
   ├─ 语速：0.9（比正常略慢，更清晰）
   └─ 音量：1.0（最大）
   ↓
6. 按钮状态变化：
   ├─ 开始播放：显示"playing"动画（脉冲效果）
   ├─ 已禁用：无法重复点击
   ├─ 播放完成：恢复可点击状态
   └─ 错误：显示浏览器兼容性提示
```

### 完整示例对话
```
用户：我在这家公司干了五年，就因为怀孕被调岗降薪。
      我是不是太不幸了，这辈子完了？

系统回复：

【🧠 情绪识别】
我听到了你的声音。你现在感受到的情绪包括：无力感、受伤感、绝望
这些情绪是正常的反应，值得被重视。

【⚡ 认知模式识别】
在你的描述中，我注意到了这些常见的思维模式：
1. 绝对化思维：用过于绝对的语言（应该、必须、永远）
2. 灾难化思维：倾向于预期最糟的结果
3. 过度自责：承担过多的责任

【💡 重新审视】
让我们一起来温和地审视这些想法：
• 这个想法是100%准确的吗？有没有其他的可能性？
• 如果这件事发生在你信任的朋友身上，你会怎么看待它？
• 在这种情况下，你的优点和资源有哪些？

【🌈 新的视角】
尝试用这个更平衡的观点：
"这是一个挑战，而不是一个永久的状态。你有能力适应和改变。"

【🎯 今天可以尝试】
• 1个小行动：今天做一件让自己感到舒服的事（走路、听音乐、和朋友聊天）
• 2个观察：注意今天发生的两件好事，无论多小
• 3次呼吸：当感受强烈时，做3次深呼吸，给自己停顿

【💚 温暖的话】
"你正在经历困难，但这不意味着你很弱。相反，你正在勇敢地面对它。
每一个小的改变都是值得庆祝的。你值得被善待，首先要对自己善待。"

如果这些感受继续困扰你，寻求专业心理咨询师的帮助是一个很好的选择。

[旁边有按钮] 🎧 语音引导 [点击播放上述内容]
```

### 关键函数
- `generateCBTResponse(content)` - 生成CBT结构回复
- `detectEmotions(text)` - 识别情绪
- `detectThinkingPatterns(text)` - 识别非理性思维
- `generateBalancedThought(content)` - 生成平衡观点
- `generateActionSteps(content)` - 生成行动步骤
- `playVoiceGuide(recordId, text)` - 播放语音引导

### 浏览器兼容性
- ✅ Chrome/Edge 14+
- ✅ Firefox 44+
- ✅ Safari 14.1+
- ❌ IE（不支持）

### 语音引导注意事项
- 语言限制为中文简体（zh-CN）
- 支持所有系统（Windows、Mac、iOS、Android）
- 在某些浏览器中可能需要用户允许语音权限
- 如不支持，会显示提示并提供备选方案

---

## 技术架构

### 代码分布
```
js/
├── evidence.js
│   ├── SHA-256相关函数 (~130行)
│   ├── 证书卡片生成 (~80行)
│   ├── ToDoList渲染 (~70行)
│   └── 文件上传处理 (~100行)
├── harbor.js
│   ├── 危机关键词表 (~20行)
│   ├── 关键词检测 (~20行)
│   ├── 危机弹窗 (~60行)
│   ├── CBT生成函数 (~100行)
│   ├── 情绪/思维检测 (~70行)
│   └── 语音引导 (~60行)
└── ui-common.js
    └── 定制化renderHistory (修改)

css/
└── style.css
    ├── 证书卡片样式 (~80行)
    ├── ToDoList样式 (~60行)
    ├── 危机弹窗样式 (~80行)
    └── 语音按钮样式 (~30行)
```

### 新增API使用
- `crypto.subtle.digest()` - Web Crypto API
- `speechSynthesis.speak()` - Web Speech API
- `navigator.clipboard.writeText()` - Clipboard API
- `URL.createObjectURL()` - Blob操作

### 存储方案
- localStorage：历史记录存储（modeName + "History"）
- sessionStorage：暂无使用
- IndexedDB：暂无使用

---

## 测试清单

### 3.1 SHA-256 证据指纹测试
- [ ] 上传文本文件，确认生成SHA-256
- [ ] 上传图片文件，确认生成SHA-256
- [ ] 上传大文件（>10MB），性能测试
- [ ] 修改文件内容，确认哈希值变化
- [ ] 下载JSON凭证，确认格式正确
- [ ] 点击截图，确认功能（或文本复制）
- [ ] 删除卡片，确认消失
- [ ] 多个文件上传，确认互不影响
- [ ] 拖拽上传，确认有效
- [ ] 点击区域上传，确认有效

### 3.2 结构化取证清单测试
- [ ] 查询取证指南，确认自动转换为清单
- [ ] 检查清单项目数是否正确提取
- [ ] 勾选第一项，进度条更新为1/N
- [ ] 勾选所有项，进度条显示100%
- [ ] 完成时，显示庆祝提示"🎉"
- [ ] 取消勾选，进度条数值减少
- [ ] 复制历史记录，确认内容完整
- [ ] 删除历史记录，确认清单消失
- [ ] 不同场景的查询，确认清单不同

### 3.3 危机干预弹窗测试
- [ ] 输入"自杀"，立即显示弹窗
- [ ] 输入"绝望"，立即显示弹窗
- [ ] 输入"不想活"，立即显示弹窗
- [ ] 输入"自伤"，立即显示弹窗
- [ ] 验证三条热线号码正确
- [ ] 点击背景，不能关闭（应提示）
- [ ] 点击"✕"按钮，弹窗关闭
- [ ] 关闭后再输入关键词，重新弹出
- [ ] 验证热线可拨打（上线前）
- [ ] 弹窗z-index足够高，不被其他内容遮挡

### 3.4 CBT+语音引导测试
- [ ] 进行倾诉，确认回复包含CBT结构
- [ ] 检查【情绪识别】部分是否正确
- [ ] 检查【认知识别】部分是否识别思维模式
- [ ] 点击"🎧 语音引导"，确认开始播放
- [ ] 确认语音是中文
- [ ] 确认语速适中（0.9倍速）
- [ ] 播放完成后按钮恢复可点击状态
- [ ] 播放中点击按钮，不重复启动
- [ ] 不支持的浏览器，显示兼容性提示
- [ ] 多条历史记录，每条都有语音按钮
- [ ] 语音内容格式正确（去除markdown）

---

## 部署清单

### 上线前检查
- [ ] 所有函数无语法错误（已通过）
- [ ] 热线号码已逐一核实可用
- [ ] 关键词表是否需要根据地域调整
- [ ] 测试所有浏览器兼容性
- [ ] 测试移动设备（iOS/Android）
- [ ] 性能测试（大文件上传）
- [ ] 无障碍测试（屏幕阅读器）
- [ ] 隐私测试（信息不上传到云）

### 可选后续优化
1. **3.1** 集成 html2canvas 实现真正的截图功能
2. **3.2** 智能体返回JSON格式以提高解析准确性
3. **3.3** 集成第三方心理援助API更新热线
4. **3.4** 调整CBT提示词给智能体，获得更专业回复

---

## FAQ

**Q: 为什么SHA-256计算需要几秒钟？**
A: 大文件需要读取到内存并进行哈希计算。这是正常的。

**Q: 关键词检测会不会误触？**
A: 可能会。例如在讨论小说情节时提到"自杀"也会触发。这是为了安全起见的权衡。

**Q: 语音引导不工作怎么办？**
A: 检查浏览器是否支持Web Speech API。如不支持，系统会自动复制文本到剪贴板。

**Q: 凭证JSON文件有什么用？**
A: 可以在法律诉讼中作为证据提交，证明文件在特定时间的哈希值。

---

## 联系与反馈
如有任何问题或建议，请反馈给开发团队。
