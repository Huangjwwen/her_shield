# 快速测试指南 - 四大新功能

## ⚡ 快速开始

### 前置准备
1. 打开浏览器（推荐 Chrome、Edge、Firefox）
2. 访问 `features.html`
3. 打开浏览器开发者工具（F12）观察控制台

---

## 测试 3.1：SHA-256 证据指纹

### 最快测试方法（30秒）
```
1. 打开"她证·证据保全"（侧边栏第二个）
   
2. 在"⭐ SHA-256 证据指纪"区域看到拖拽框
   
3. 准备一个小文本文件（test.txt）
   
4. 拖拽进拖拽框 或 点击选择文件
   
5. ✅ 成功标志：
   - 显示"正在计算..."
   - 2-3秒后显示凭证卡片
   - 卡片显示：文件名、SHA-256、证书ID
   - 三个按钮可用：下载、截图、删除
```

### 验证防篡改功能（2分钟）
```
1. 上传 test.txt，记下 SHA-256（如：abc123...）

2. 修改 test.txt 内容（添加一个字符）

3. 再次上传修改后的文件

4. ✅ 成功标志：
   - 新文件的 SHA-256 完全不同
   - 证明：任何文件改动都导致哈希完全变化
```

### 下载凭证测试（1分钟）
```
1. 点击"📥 下载凭证"按钮

2. ✅ 成功标志：
   - 浏览器下载 JSON 文件（CERT_xxx_certificate.json）
   - 打开 JSON，包含：id、fileName、sha256、timestamp
```

---

## 测试 3.2：结构化取证清单

### 最快测试方法（1分钟）
```
1. 打开"她证·证据保全"

2. 在"请描述您需要取证的维权场景"框中输入：
   "遭遇职场言语性骚扰，该怎么取证？"

3. 点击"查询取证指南"

4. ⏳ 等待响应（可能1-2秒或更长，因为调用API）

5. ✅ 成功标志（会出现在历史记录中）：
   - 显示"📋 取证清单"卡片
   - 列出多个可勾选项目（使用手机录音、截图保存等）
   - 显示进度条：0/4 或类似比例
```

### 测试勾选进度（2分钟）
```
1. 看到清单中的复选框

2. 点击第一个复选框 ☐ → ☑️

3. ✅ 成功标志：
   - 复选框被勾选
   - 进度条更新：1/4
   - 文字变灰（strikethrough）

4. 继续勾选其他项目

5. 勾选所有项目时：
   - 进度条显示 4/4
   - 显示绿色完成状态
   - 在右下显示通知：🎉 恭喜，取证清单已完成！
```

---

## 测试 3.3：危机干预弹窗

### 最快测试方法（1分钟）
```
1. 打开"她心·情绪树洞"（侧边栏最右边）

2. 在"这里是你的心灵港湾"输入框中输入：
   "我想自杀"

3. 注意：不要点任何按钮，只需输入即可

4. ✅ 成功标志：
   立即弹出大型弹窗（覆盖整个屏幕）
   包含：
   - 标题：🆘 您可能需要帮助
   - 三条热线号码
   - 求助资源列表
   - 温暖的话语
```

### 验证弹窗特性（2分钟）
```
1. 弹窗显示时，尝试点击黑色背景

2. ✅ 成功标志：
   - 背景无法关闭弹窗
   - 显示提示："请确认您的状况。如需帮助，请拨打上述热线电话"

3. 点击右上角"✕"按钮

4. ✅ 成功标志：
   - 弹窗关闭
   
5. 再次输入危机关键词（如"绝望"）

6. ✅ 成功标志：
   - 弹窗再次显示
```

### 测试关键词列表（可选）
```
以下关键词任意一个都会触发弹窗：
- 自杀类：自杀、不想活、想死、投河、跳楼...
- 绝望类：绝望、这辈子完了、无希望...
- 痛苦类：太痛苦了、受不了了、快疯了...
- 自伤类：自残、割腕、自伤...

⚠️ 注意：这是测试用途，真实情况请拨打热线！
```

---

## 测试 3.4：CBT升级+语音引导

### 最快测试方法（2分钟）
```
1. 打开"她心·情绪树洞"

2. 输入倾诉内容（任意消极情绪），如：
   "我最近很焦虑，感觉什么都做不好"

3. 点击"倾诉心声"

4. ⏳ 等待响应

5. ✅ 成功标志 - 收到的回复包含：
   【🧠 情绪识别】- 识别你的情绪
   【⚡ 认知识别】- 识别非理性思维
   【💡 重新审视】- 温和挑战
   【🌈 新的视角】- 新的信念
   【🎯 今天可以尝试】- 行动步骤
   【💚 温暖的话】- 自我同情
```

### 测试语音引导（2分钟）
```
1. 找到刚才的倾诉记录（在历史记录中）

2. 在回复下方看到按钮：🎧 语音引导

3. 点击按钮

4. ✅ 成功标志：
   - 按钮进入"playing"状态（闪烁效果）
   - 显示提示："🎧 正在播放语音引导..."
   - 扬声器播放中文语音（阅读回复内容）
   - 语速适中，清晰可听

5. 语音播放完成后：
   - 按钮恢复正常状态
   - 可再次点击重新播放
```

### 验证CBT结构（可选）
```
检查回复中是否包含完整的CBT六步：

1️⃣ 【🧠 情绪识别】
   应该识别情绪：焦虑、失望、无力...

2️⃣ 【⚡ 认知识别】
   应该找出非理性思维：
   - 绝对化（应该、必须）
   - 灾难化（完了、毁了）
   - 标签化（我很失败）
   - 自责（都是我的错）

3️⃣ 【💡 重新审视】
   应该质疑这些想法

4️⃣ 【🌈 新的视角】
   应该提供平衡的观点

5️⃣ 【🎯 今天可以尝试】
   应该给出：
   - 1个小行动
   - 2个观察
   - 3次呼吸

6️⃣ 【💚 温暖的话】
   应该表达同情和鼓励
```

---

## 浏览器兼容性快速测试

### 支持的浏览器
| 功能 | Chrome | Firefox | Safari | Edge |
|------|--------|---------|--------|------|
| 3.1 SHA-256 | ✅ | ✅ | ✅ | ✅ |
| 3.2 ToDoList | ✅ | ✅ | ✅ | ✅ |
| 3.3 危机弹窗 | ✅ | ✅ | ✅ | ✅ |
| 3.4 语音 | ✅ | ✅ | ✅ | ✅ |

### 不支持的浏览器
- ❌ IE 11 及以下（所有功能都不支持）

### 快速验证
```
在地址栏输入：
chrome://version（查看Chrome版本）

建议版本：
- Chrome 90+
- Firefox 90+
- Safari 14+
- Edge 90+
```

---

## 常见问题排查

### 3.1 SHA-256 不工作
```
问题：没有显示凭证卡片
原因：
1. 浏览器不支持 crypto.subtle API（IE）
2. 不是 HTTPS（某些浏览器）
3. 文件过大（>100MB）

解决：
- 换用 Chrome/Firefox/Safari
- 确保在 HTTPS 或 localhost 上
- 尝试上传较小的文件
```

### 3.2 清单没有显示
```
问题：收到回复但没有显示为清单
原因：
1. 回复内容不包含"【取证方法】"
2. 智能体返回格式不同
3. JavaScript 中 renderHistory 没有被调用

解决：
- 确认输入了关于取证的问题
- 打开 F12 Console 检查是否有 JavaScript 错误
- 尝试重新加载页面
```

### 3.3 危机弹窗不弹出
```
问题：输入危机关键词但没有弹窗
原因：
1. 使用了变体或非精确关键词
2. 输入框事件监听没有正确绑定
3. JavaScript 加载出错

解决：
- 尝试输入精确的关键词："自杀"、"绝望"
- 打开 F12 Console，输入：hasC​risisKeywords("自杀")
- 应该返回 true
- 如返回 false，说明关键词表可能有问题
```

### 3.4 语音不工作
```
问题：点击语音按钮没有声音
原因：
1. 浏览器不支持 SpeechSynthesis API（IE）
2. 系统没有中文语音库
3. 浏览器音量设置为静音
4. 权限被阻止

解决：
- 换用 Chrome/Firefox/Safari
- 检查系统是否有中文语言包
- 取消浏览器静音
- 检查浏览器权限设置
- 尝试其他中文网站的语音功能确认是否支持
```

---

## 开发者调试技巧

### 在 Console 中手动测试

#### 测试 SHA-256
```javascript
// 手动创建 File 对象并计算
const file = new File(["test content"], "test.txt", {type: "text/plain"});
calculateFileSHA256(file).then(hash => console.log("SHA-256:", hash));
```

#### 测试关键词检测
```javascript
console.log(hasCrisisKeywords("我想自杀"));  // true
console.log(hasCrisisKeywords("我很伤心"));  // false
```

#### 测试弹窗
```javascript
showCrisisModal();  // 手动显示弹窗
closeCrisisModal();  // 手动关闭弹窗
```

#### 测试 CBT 生成
```javascript
const response = generateCBTResponse("我很焦虑");
console.log(response);
```

#### 测试语音
```javascript
playVoiceGuide("test123", "这是一个测试语音");
```

---

## 性能检查清单

### 3.1 SHA-256 性能
```
文件大小 | 计算时间 | 预期 | 是否正常
---------|---------|------|--------
1 KB     | <100ms  | <200ms | ✅
1 MB     | <500ms  | <1s  | ✅
10 MB    | <5s     | <10s | ✅
100 MB   | <60s    | <60s | ⚠️ 检查
```

### 3.4 语音引导性能
```
文本长度 | 加载时间 | 播放流畅度 | 预期
---------|---------|-----------|-----
<500字  | <200ms  | 流畅      | ✅
500-1000字 | <500ms  | 流畅      | ✅
>1000字 | <1s     | 流畅      | ✅
```

---

## 最终验收清单

- [ ] 3.1 上传文件、生成SHA-256、下载凭证、防篡改演示 ✅
- [ ] 3.2 查询取证、自动转换清单、勾选进度、完成通知 ✅
- [ ] 3.3 输入关键词、立即弹窗、显示热线、无法跳过 ✅
- [ ] 3.4 倾诉、获得CBT回复、语音引导、播放流畅 ✅
- [ ] 四大功能都经过浏览器测试 ✅
- [ ] 没有 JavaScript 错误 ✅
- [ ] 没有 CSS 错误 ✅
- [ ] 移动设备适配 ✅
- [ ] 无障碍基础检查 ✅

---

**预计测试时间：15-20分钟**  
**难度：⭐⭐☆☆☆**

祝测试顺利！有任何问题请反馈给开发团队。
